from setuptools import setup, find_packages

setup(
    name='Realtime_chat',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        "Django~=4.0.1",
        "channels~=3.0.4"
    ],
    entry_points='''

    ''',
)
